name: "Code Mood Ring"
description: "Analyzes PR commits and code comments and posts the developer's emotional state. Completely useless. Highly accurate."
author: "cadamsmith"

branding:
  icon: "activity"
  color: "purple"

inputs:
  github-token:
    description: "GitHub token for posting PR comments"
    required: false
    default: ${{ github.token }}

runs:
  using: "node20"
  main: "dist/index.js"
